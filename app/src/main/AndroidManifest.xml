<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".StudentProvider"></activity>
        <activity android:name=".CreateOrEditActivity" />
        <activity android:name=".Save_To_Space" />
        <activity android:name=".SetAlarm" />
        <activity android:name=".Alarm" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Read" />
        <activity android:name=".Songs" />
        <activity android:name=".DisplayMessageActivity" />
        <activity android:name=".MainActivity" />

        <receiver
            android:name=".AlarmReciever"
            android:enabled="true"
            android:exported="true" />

        <service
            android:name=".MusicService"
            android:enabled="true" />
        <provider
            android:authorities="com.example.myapplication.StudentProvider"
            android:name=".StudentsProvider"/>
    </application>

</manifest>